{% extends 'base.html.twig' %}

{% block title 'Welcome' %}

{% block body %}
    <div class="wrapper">
      <div class="abs-center wd-xl">
         <!-- START card-->
         <div class="d-flex justify-content-center">
            <div class="p-2">Dernière partie !</div>
         </div>
         <div class="card b0">
            <div class="card-body">
               <p class="text-center">Configurez votre compte utilisateur</p>
               {{ form_start(form) }}
                        {# {{ form_row(form.name) }} #}
                    {{ form_rest(form) }}
                    <button class="btn btn-block btn-primary mt-3 col-lg-4 float-right" type="submit" formnovalidate>Terminer</button>
                {{ form_end(form) }}
            </div>
         </div><!-- END card-->
         {% include "_short_footer.html.twig" %}
      </div>
   </div>
{% endblock %}
{# Logo institutionnel AUCTC (Union Africaine) #}
<img
    src="{{ asset('img/auctc-logo.png') }}"
    alt="{{ 'brand.name'|trans }}"
    class="caert-brand-logo{% if brand_logo_class is defined %} {{ brand_logo_class }}{% endif %}"
    width="36"
    height="51"
    decoding="async"
>
